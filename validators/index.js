const { validateAddStaker } = require('./validateAddStaker');
const { validateUnStake } = require('./validateUnStake');
validateUnStake;

module.exports = {
    validateAddStaker,
    validateUnStake,
};
